# docker-compose.yml
#
# Local development environment for proxypay.
# Starts the app alongside Postgres, Redis, and local mock services for Stellar and SMTP.
#
# Usage:
#   docker compose up          # start everything
#   docker compose up -d       # start in background
#   docker compose down        # stop and remove containers
#   docker compose down -v     # stop and wipe volumes (fresh DB)

version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: proxypay_app
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      maildev:
        condition: service_healthy
      stellar:
        condition: service_healthy
    env_file:
      - .env
    environment:
      NODE_ENV: development
      LOG_LEVEL: ${LOG_LEVEL:-info}
      DATABASE_URL: postgresql://user:password@postgres:5432/proxypay_stellar
      REDIS_URL: redis://redis:6379
      SMTP_HOST: maildev
      SMTP_PORT: 1025
      STELLAR_HORIZON_URL: http://stellar:8000
      STELLAR_NETWORK: local
      # Loki transport — optional, falls back to stdout when unset
      LOKI_HOST: ${LOKI_HOST:-http://loki:3100}
      SERVICE_NAME: ${SERVICE_NAME:-proxypay-api}
    ports:
      - "3000:3000"
    networks:
      - proxypay_net
    logging:
      driver: "json-file"
      options:
        tag: "{{.Name}}"

  postgres:
    image: postgres:16-alpine
    container_name: proxypay_postgres
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: proxypay_stellar
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U user -d proxypay_stellar"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - proxypay_net

  # ---------------------------------------------------------------------------
  # Postgres streaming read replica (hot standby) — simulates a cross-region
  # replica locally for lag monitoring / failover testing. Configure the app
  # with READ_REPLICA_URL=postgresql://user:password@postgres-replica:5432/proxypay_stellar
  # ---------------------------------------------------------------------------
  postgres-replica:
    image: postgres:16-alpine
    container_name: proxypay_postgres_replica
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: proxypay_stellar
      PRIMARY_HOST: postgres
      REPLICA_USER: user
      PGPASSWORD: password
    entrypoint: ["/usr/local/bin/pg-replica-entrypoint.sh"]
    volumes:
      - ./scripts/pg-replica-entrypoint.sh:/usr/local/bin/pg-replica-entrypoint.sh:ro
      - postgres_replica_data:/var/lib/postgresql/data
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U user -d proxypay_stellar"]
      interval: 5s
      timeout: 5s
      retries: 5
      start_period: 20s
    networks:
      - proxypay_net

  redis:
    image: redis:7-alpine
    container_name: proxypay_redis
    command: redis-server /usr/local/etc/redis/redis.conf
    volumes:
      - ./redis.conf:/usr/local/etc/redis/redis.conf:ro
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
    networks:
      - proxypay_net

  # ---------------------------------------------------------------------------
  # Mock SMTP Server (Maildev)
  # ---------------------------------------------------------------------------
  maildev:
    image: maildev/maildev:2.1.0
    container_name: proxypay_maildev
    restart: unless-stopped
    ports:
      - "1080:1080" # Web UI
      - "1025:1025" # SMTP Port
    healthcheck:
      test: ["CMD-SHELL", "nc -z localhost 1025 || exit 1"]
      interval: 5s
      timeout: 3s
      retries: 5

  # ---------------------------------------------------------------------------
  # Local Stellar Quickstart Node
  # ---------------------------------------------------------------------------
  stellar:
    image: stellar/quickstart:latest
    container_name: proxypay_stellar_quickstart
    restart: unless-stopped
    ports:
      - "8000:8000" # Horizon API / Faucet (Friendbot)
    command: ["--standalone", "--enable-horizon"]
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8000 || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s

  # ---------------------------------------------------------------------------
  # Loki (centralized log aggregation)
  # ---------------------------------------------------------------------------
  loki:
    image: grafana/loki:latest
    container_name: proxypay_loki
    restart: unless-stopped
    ports:
      - "3100:3100"
    volumes:
      - ./logging/loki-config.yaml:/etc/loki/local-config.yaml
    command: -config.file=/etc/loki/local-config.yaml
    networks:
      - proxypay_net

  promtail:
    image: grafana/promtail:latest
    container_name: proxypay_promtail
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./logging/promtail-config.yaml:/etc/promtail/config.yml
    command: -config.file=/etc/promtail/config.yml
    networks:
      - proxypay_net

  grafana:
    image: grafana/grafana:latest
    container_name: proxypay_grafana
    ports:
      - "3001:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
      - GF_PATHS_PROVISIONING=/etc/grafana/provisioning
    volumes:
      - grafana_data:/var/lib/grafana
      - ./logging/grafana/provisioning:/etc/grafana/provisioning
    networks:
      - proxypay_net

networks:
  proxypay_net:
    driver: bridge

volumes:
  grafana_data:
  redis_data:
  postgres_replica_data:
